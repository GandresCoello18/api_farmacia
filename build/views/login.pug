- var titulo =  'LOGIN | Farmacia Juanito'
include head.pug
body
    section.container-fluid.p-5
        .row.justify-content-center
            .col-12.col-md-6.col-lg-4

              - if (feeback == "Autentiando....")
                  .alert.alert-success.p-1
                    strong= feeback
              - else if (feeback != "")
                  .alert.alert-danger.p-1
                    strong= feeback

              h3.p-3.text-center Ingrese con las mismas credenciales
              form.p-4(action="/view/login", method='post')
                input(type="email" placeholder="admin@gmail.com" name='email').form-control
                input(type="password", placeholder='**********' name='password').form-control.mt-2
                button.btn.btn-success.form-control.mt-2 Entrar
              
              p.p-5.text-danger * Solo podras acceder si eres actualmente "Administrador"